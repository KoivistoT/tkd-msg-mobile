import io from "socket.io-client";
import settings from "../config/settings";

// export const createSocketConnection = async () => {
//   const socket = await io.connect(settings.baseUrl, {
//     transports: ["websocket"],
//     // jsonp: false,
//   });
//   return socket;
// };
